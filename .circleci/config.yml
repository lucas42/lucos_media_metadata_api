version: 2.0
jobs:
  test:
    docker:
      - image: circleci/golang:1
    working_directory: /go/src/github.com/lucas42/lucos_media_metadata_api

    steps:
      - checkout
      - run:
          name: Unit Tests
          command: go test
workflows:
 version: 2
 Test:
   jobs:
    - test